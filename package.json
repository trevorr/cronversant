{
  "name": "cronversant",
  "version": "1.0.0",
  "description": "Cron expression parser and formatter",
  "main": "src/index.js",
  "files": [
    "src"
  ],
  "scripts": {
    "test": "npm run lint && mocha --recursive test",
    "test:watch": "mocha --recursive test --watch",
    "lint": "eslint --max-warnings 0 'src/**/*.js'",
    "lint:fix": "eslint --fix 'src/**/*.js'"
  },
  "husky": {
    "hooks": {
      "pre-commit": "npm test",
      "pre-push": "npm test"
    }
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/trevorr/cronversant.git"
  },
  "author": {
    "name": "Trevor Robinson"
  },
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/trevorr/cronversant/issues"
  },
  "homepage": "https://github.com/trevorr/cronversant#readme",
  "dependencies": {},
  "devDependencies": {
    "chai": "^4.2.0",
    "eslint": "^5.16.0",
    "husky": "^2.7.0",
    "mocha": "^6.1.4"
  }
}
